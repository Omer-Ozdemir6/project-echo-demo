{
  "elias_discovers_lie": {
    "type": "delayed",

    "triggerAfterEpisodes": 3,

    "requiredFlags": [
      "liedToElias"
    ],

    "onlyOnce": true
  },

  "abandonment_argument": {
    "type": "delayed",

    "triggerAfterEpisodes": 2,

    "requiredFlags": [
      "abandonedElias"
    ],

    "onlyOnce": true
  },

  "loyalty_scene": {
    "type": "relationship",

    "triggerAfterEpisodes": 2,

    "requiredFlags": [
      "savedElias"
    ],

    "minimumTrust": 60,

    "onlyOnce": true
  },

  "possible_rebellion": {
    "type": "relationship",

    "triggerAfterEpisodes": 4,

    "requiredFlags": [
      "liedToElias"
    ],

    "minimumResentment": 60,

    "onlyOnce": true
  }
}